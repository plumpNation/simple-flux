document.addEventListener('DOMContentLoaded', init);

function init() {
    // Setup one Accordion
    const
        accordionElement = document.getElementById('my-accordion'),
        accordion1 = new Accordion(accordionElement);
}
